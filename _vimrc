" 行番号表示(set nonumberで非表示になる。コピペ時に利用) 
set number

" 画面上でタブ文字が占める幅
set tabstop=2

" 自動インデントでずれる幅
set shiftwidth=2

" タブ入力を複数の空白入力に置き換える(既存タブには影響なし) 
set expandtab

" 現在の行を強調表示
set cursorline

" ステータスラインを常に表示
set laststatus=2

" 検索語をハイライト表示
set hlsearch
